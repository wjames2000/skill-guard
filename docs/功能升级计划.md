# skill-guard 功能升级计划

版本 2.0 · 2026-04-29

---

## 一、版本状态快照

| 版本 | 状态 | 提交 |
|------|------|------|
| v0.1.0 (初始) | ✅ 发布 | 86d2c1f |
| v1.1 (质量加固) | ✅ 完成 | 349ce12 |
| v1.2 (体验增强) | ✅ 完成 | fd847d6 |
| v2.0 (生态扩展·上半) | ✅ 完成 | 1dcafde |

---

## 二、功能完成度审计

### 2.1 需求覆盖率

| 需求 | 状态 | 备注 |
|------|------|------|
| FR-1 目录扫描 | ✅ 完成 | 多目录、递归、ignore + `.gitignore` 模式 |
| FR-2 文件类型支持 | ✅ 完成 | 扩展名包含/排除/Symlink 跳过 |
| FR-3 规则检测引擎 | ✅ 完成 | 32 条内置规则，5 大类别，panic 保护 |
| FR-4 自定义规则 | ✅ 完成 | YAML/JSON 加载，覆盖/追加 |
| FR-5 输出格式 | ✅ 完成 | 终端彩色/JSON/Quiet/Summary/SARIF |
| FR-6 退出码 | ✅ 完成 | 0/1/2 + `--output` 文件导出 |
| FR-7 配置管理 | ✅ 完成 | `.skillguard.yaml` + CLI 合并 |
| FR-8 扫描性能 | ✅ 完成 | Worker Pool + `--concurrency` |

### 2.2 非功能需求覆盖率

| 需求 | 状态 | 备注 |
|------|------|------|
| NFR-1 零依赖部署 | ✅ 完成 | 单二进制 3.8MB |
| NFR-2 跨平台 | ✅ 完成 | 5 平台交叉编译 + brew/curl-sh/scoop |
| NFR-3 10000 文件内存 < 50 MB | ⚠️ 未验证 | 缺少生产环境基准 |
| NFR-4 1000 文件比 Node.js 快 2 倍 | ⚠️ 未验证 | 缺少对标测试 |
| NFR-5 单规则 < 50ms | ✅ 验证 | 基准测试 ~0.14ms/op，远低于阈值 |
| NFR-6 大文件线性能耗 | ✅ 验证 | 50MB 二进制 0.045s 正确跳过 |
| NFR-7 易安装 | ✅ 完成 | brew / curl-sh / scoop / GitHub Releases |
| NFR-8 错误容忍 | ✅ 完成 | 单文件失败跳继续扫描 |
| NFR-9 正则 panic 保护 | ✅ 完成 | matcher.go defer/recover |
| NFR-10 进度可观测 | ✅ 完成 | `--verbose` `\r` 单行刷新 |
| NFR-11 不执行扫描文件 | ✅ 完成 | 只读分析 |
| NFR-12 无网络请求 | ✅ 完成 | 扫描完全离线（`update` 命令除外） |
| NFR-13 测试覆盖 ≥ 85% | ⚠️ 部分 | scanner 7/6 ✅，其余包尚缺 |
| NFR-14 规则可扩展 | ✅ 完成 | 接口化设计 + 自定义规则 + update 命令 |
| NFR-15 文档完善 | ✅ 完成 | README、内置帮助、产品网站、安装脚本 |

### 2.3 测试覆盖 vs 目标

| 包 | 当前数 | 目标 | 差距 | 建议新增场景 |
|----|--------|------|------|-------------|
| `internal/scanner` | **7** | ≥6 | ✅ 达标 | — |
| `internal/engine` | 10 | ≥15 | ❌ -5 | 大文件、并发场景、空规则 |
| `internal/file` | 7 | ≥10 | ❌ -3 | symlink 测试、超大文件、非 UTF-8 目录 |
| `internal/output` | 5 | ≥7 | ❌ -2 | SeverityFilter 边界、空报告 |
| `internal/report` | 3 | ≥5 | ❌ -2 | 空结果、全部同级别 |
| `internal/config` | 4 | ≥6 | ❌ -2 | 文件损坏、权限拒绝 |
| `pkg/types` | 2 | ≥3 | ❌ -1 | Config.Severity 大小写不敏感 |
| `rules` | 38 | ≥96 | ❌ -58 | 每条规则更多负例 |

---

## 三、已实现功能清单

### v1.1 — 质量加固（已完成）

| 任务 | 状态 | 文件 |
|------|------|------|
| Scanner 单元测试 (7用例) | ✅ | `internal/scanner/scanner_test.go` |
| 正则 panic 保护 | ✅ | `internal/engine/matcher.go` |
| 性能基准测试 (4 bench) | ✅ | `internal/engine/bench_test.go`, `internal/scanner/bench_test.go` |
| Windows 颜色兼容 | ✅ | `internal/output/terminal.go` |
| Symlink 跳过 | ✅ | `internal/file/discover.go` |
| 进度条 `\r` 单行刷新 | ✅ | `internal/scanner/scanner.go` |
| `.gitignore` 自动识别 | ✅ | `internal/file/filter.go` |

### v1.2 — 体验增强（已完成）

| 任务 | 状态 | 文件 |
|------|------|------|
| Shell 自动补全 (bash/zsh/fish) | ✅ | `cmd/completion.go` |
| `--no-color` 参数 | ✅ | `cmd/root.go`, `internal/output/terminal.go` |
| `--output` 结果导出 | ✅ | `cmd/root.go`, `pkg/types/config.go` |
| `--summary` 摘要模式 | ✅ | `internal/output/output.go` |
| SARIF 格式输出 | ✅ | `internal/output/sarif.go` |

### v2.0 — 生态扩展（部分完成）

| 任务 | 状态 | 文件 |
|------|------|------|
| `skill-guard update check/rules` | ✅ | `cmd/update.go`, `internal/updater/updater.go` |
| Pre-commit Hook | ✅ | `.pre-commit-config.yaml`, `.hooks/skill-guard-pre-commit` |
| 规则市场 | ⏳ 未开始 | — |
| AI 辅助检测 | ⏳ 未开始 | — |
| LUA 脚本规则 | ⏳ 未开始 | — |
| IDE 插件 (VS Code/JetBrains) | ⏳ 未开始 | — |

---

## 四、后续升级路线

### Sprint 3 — 测试覆盖加固（1 周）

| 优先级 | 任务 | 说明 | 涉及文件 |
|--------|------|------|---------|
| P0 | Engine 补充测试（+5） | 大文件、并发、空规则场景 | `internal/engine/engine_test.go` |
| P0 | File 补充测试（+3） | symlink、超大文件、非 UTF-8 目录 | `internal/file/` |
| P1 | Output 补充测试（+2） | SeverityFilter 边界 | `internal/output/output_test.go` |
| P1 | Report 补充测试（+2） | 空结果、同级别排序 | `internal/report/aggregate_test.go` |
| P1 | Config 补充测试（+2） | 文件损坏、无权限 | `internal/config/config_test.go` |
| P2 | Types 补充测试（+1） | Validate 大小写不敏感 | `pkg/types/config_test.go` |

### Sprint 4 — v2.0 核心功能（2 周）

| 优先级 | 任务 | 说明 | 复杂度 |
|--------|------|------|--------|
| P1 | **规则市场** — 规则索引文件 | 设计 `rules-index.yaml` 结构，`update` 命令下载索引 + 按需下载规则 | 中 |
| P1 | **AI 辅助检测** — Ollama 集成 | `--ai` 参数调用本地 Ollama API 分析可疑内容，降低误报 | 高 |
| P2 | **Pre-commit 自动安装** | `skill-guard init` 命令一键配置 git hooks | 低 |

### Sprint 5 — v2.0 高级功能（远期）

| 任务 | 说明 | 复杂度 |
|------|------|--------|
| LUA 脚本规则 | Go LUA VM 嵌入，支持脚本化检测逻辑 | 高 |
| VS Code 插件 | 以 LSP 协议集成扫描结果到编辑器 | 高 |
| JetBrains 插件 | 同上，IntelliJ Platform 插件 | 高 |
| GitHub Action | 官方 Action 一键集成 CI/CD | 中 |

---

## 五、版本时间线

```
v0.1.0 ───── v1.1 ───── v1.2 ───── v2.0-α ───── v2.0-β ───── v2.0
   基础功能    质量加固     体验增强     测试覆盖     规则市场     AI + IDE
   2026.4.29    已完        已成      当前阶段      下阶段       远期
```

## 六、关键指标

| 指标 | 当前值 | 目标 |
|------|--------|------|
| Go 源文件数 | 50 | — |
| 测试用例数 | 46 | ≥80 |
| 内置规则数 | 32 | ≥50 |
| 二进制大小 | 3.8 MB | ≤5 MB |
| 100 文件扫描 | 8.3 ms | ≤10 ms |
| gosec issues | 0 | 0 |
| GitHub Stars | — | ≥100 |

---

*本文档基于实际项目代码审计生成，版本 2.0 反映了截至当前的所有变更。*
